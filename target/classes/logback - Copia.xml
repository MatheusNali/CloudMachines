<configuration>

  <appender name="FILE_HEAD" class="ch.qos.logback.core.FileAppender">
    <file>logs/CloudMachines.xml</file>
	<filter class="ch.qos.logback.core.filter.EvaluatorFilter">
		<evaluator class="ch.qos.logback.classic.boolex.JaninoEventEvaluator">
			<expression>return !message.isEmpty() &amp;&amp; level &gt;= WARN;</expression>
		</evaluator>
		<OnMatch>NEUTRAL</OnMatch>
		<OnMismatch>DENY</OnMismatch>
	</filter>
    <encoder>
      <pattern>&lt;log&gt;%n
      &lt;log_date&gt;%date&lt;/log_date&gt;%n
      &lt;log_level&gt;%level&lt;/log_level&gt;%n
      &lt;thread_logger&gt;[%thread]%logger{10}&lt;/thread_logger&gt;%n
      </pattern>
    </encoder>
  </appender>


  <appender name="FILE" class="ch.qos.logback.core.FileAppender">
    <file>logs/CloudMachines.xml</file>
	<filter class="ch.qos.logback.core.filter.EvaluatorFilter">
		<evaluator class="ch.qos.logback.classic.boolex.JaninoEventEvaluator">
			<expression>return !message.isEmpty() &amp;&amp; level &lt;= INFO;</expression>
		</evaluator>
		<OnMatch>NEUTRAL</OnMatch>
		<OnMismatch>DENY</OnMismatch>
	</filter>
    <encoder>
      <pattern>            &lt;message&gt;%n%msg%n&lt;/message&gt;%n
&lt;/log&gt;%n
      </pattern>
    </encoder>
  </appender>

  <appender name="FILE_EMPTY" class="ch.qos.logback.core.FileAppender">
    <file>logs/CloudMachines.xml</file>
	<filter class="ch.qos.logback.core.filter.EvaluatorFilter">
		<evaluator class="ch.qos.logback.classic.boolex.JaninoEventEvaluator">
			<expression>return !message.isEmpty() &amp;&amp; level &lt;= INFO;</expression>
		</evaluator>
		<OnMatch>NEUTRAL</OnMatch>
		<OnMismatch>DENY</OnMismatch>
	</filter>
	<encoder>
		<pattern>%n</pattern>
	</encoder>
  </appender>
  
  <appender name="STDOUT" class="ch.qos.logback.core.ConsoleAppender">
    <encoder>
      <pattern>%level = "%msg"%n</pattern>
    </encoder>
  </appender>

  <root level="info">
    <appender-ref ref="FILE" />
    <appender-ref ref="STDOUT" />
  </root>
</configuration>